# Project: aws-ec2-webserver

Automated web server deployment on EC2 with custom VPC, security groups, and user data scripts.

## Usage
```bash
terraform init && terraform apply -auto-approve
# Visit: terraform output web_url
terraform destroy -auto-approve
```
