# Project: aws-s3-iam

Secure S3 storage with IAM role-based access, versioning, encryption, and lifecycle rules.

## Usage
```bash
terraform init && terraform apply -auto-approve
terraform destroy -auto-approve
```
